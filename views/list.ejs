<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('nav.ejs') %>

    <% for(let i = 0; i < result.length; i++){ %> 
        <div>
            <h1><%= i + 1 %></h1>
            <h1><a href="/detail/<%= result[i]._id %>"><%= result[i].title %></a></h1>
        </div>
    <% } %>
</body>
</html>